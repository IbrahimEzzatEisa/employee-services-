export enum ObjectStatus {
    nochange = 1,
    created = 2,
    changed = 3,
    deleted = 4,
}


export enum permissionsTypes {
    open = 0,
    read = 1,
    insert = 2,
    update = 3,
    delete = 4,
    print = 5,
}

export enum PagesTypes {
    listPage = 0,
    addPage = 1,
    editPage = 2
}
